rootProject.name = "cheeta-webapp"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url = uri("https://maven.vaadin.com/vaadin-prereleases") }
    }
}
